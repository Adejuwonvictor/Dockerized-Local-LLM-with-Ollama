# Dockerized-Local-LLM-with-Ollama
A fully containerized AI stack where a local LLM runs inside Docker, a FastAPI app talks to it over an internal network, and nginx sits in front as the gateway — no external API calls, no API keys, everything self-contained.
